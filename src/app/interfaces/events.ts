export interface Event {
    year: number;
    imageUrl: string;
    done: boolean;
}

export interface EventDay {
    day: number;
    emojiStory: string;
    gitFolderName: string;
    name: string;
}